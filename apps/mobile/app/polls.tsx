import { ResourcePlaceholder } from '../components/resource-placeholder';
export default function PollsPage() {
  return (
    <ResourcePlaceholder
      title="Sondages"
      message="Les sondages publics anonymes arriveront en Phase 8 avec protections best effort contre les votes multiples."
    />
  );
}
