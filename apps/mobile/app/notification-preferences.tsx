import { ResourcePlaceholder } from '../components/resource-placeholder';
export default function PreferencesPage() {
  return (
    <ResourcePlaceholder
      title="Préférences notifications"
      message="Vos choix seront conservés localement, sans compte, puis synchronisés avec des topics non personnels."
    />
  );
}
