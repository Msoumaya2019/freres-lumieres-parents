import { ResourcePlaceholder } from '../components/resource-placeholder';
export default function DocumentsPage() {
  return (
    <ResourcePlaceholder
      title="Documents"
      message="Flyers, menus et documents publics seront disponibles sans connexion."
    />
  );
}
